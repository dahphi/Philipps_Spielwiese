---
mode: "agent"
description: "Create a new SPD APEX/PLSQL meeting-notes page from template and set the target date/title"
---

Create a new Confluence meeting-notes page in:

- `SPD / Chapter / APEX/PLSQL / Meeting Notes` (parent id `429698593`)

Use this template page id unless I provide another one:

- `480904472`

Requirements:

1. Use title format: `YYYY-MM-DD Meeting notes`.
2. Keep existing headings/body structure unless I explicitly ask for content changes.
3. Update date fields requested by me (for example `datetime` and visible date string).
4. Ensure the final page is under parent id `429698593`.
5. Return only the created page URL and final title.
