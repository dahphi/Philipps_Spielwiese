Zugang Dev WS:
PHILIPP_TEST
Admin
admin

Zugang Prod WS:
PHILIPP_TEST
admin
admin

SQL Connect DEV:
sql apex_export_user/tiger@localhost:8521/freepdb1
SQL Connect auf Prod
sql apex_export_user/tiger@localhost:8522/freepdb1

-- #######################################################
-- EXPORT
Steps für SQLCL Project:

--1. Project init
-- Connect auf Dev
sql apex_export_user/tiger@localhost:8521/freepdb1

project init -name Fastlane -schemas apex_export_user

--2. Export der Apex Apps:
project export -o apex.100
project export -o apex.101
project export -o apex.102

-- 3. Project Stage und Verify
project stage
project verify

-- 4. Release der 1.0er Version
 project release -version 1_0 -verbose

-- 5. Generierung Artifakt
project gen-artifact -version 1_0
!git add .
!git commit -m "Initiales release"
!git push

-- #################################################
--IMPORT
-- 6. Dann auf Prod Deployen:
sql apex_export_user/tiger@localhost:8522/freepdb1
project deploy -file artifact/Fastlane-1_0.zip -verbose