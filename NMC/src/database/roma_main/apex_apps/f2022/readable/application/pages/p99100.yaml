---
# ====== Page: Test autocomplete =============================
id: 99100
identification: 
  name: Test autocomplete
  alias: TEST-AUTOCOMPLETE
  title: Test autocomplete

appearance: 
  page-mode: Normal
  page-template: Theme Default
  template-options: 
  - '#DEFAULT#'

navigation-menu: 
  override-user-interface-level: false

navigation: 
  cursor-focus: Do not focus cursor
  warn-on-unsaved-changes: false

javascript: 
  function-and-global-variable-declaration: |
    var adresscheck = {};
    var currentFocus;
    var autocompleteListe;
    var suggestlisteOffen;
    
    // Auswertung keyCode:
    function isBackpaceEinfg(k) {return k == 8 || k == 46}
    function isNumber(k) {return (k >= 48 && k <= 57) || (k >= 96 && k <= 105)}
    function isLetter(k) {return k >= 65 || k == 32}
    
    
    function autocomplete(suchbereich, suchItem, suggestItem) {
      
        switch(suchbereich) {
            case 'plz':
            ///// ['mousemove', 'touchmove'].forEach(...)
                suchItem.addEventListener("keyup", function(e) { /// ersetzen durch "input" anstatt "keyup"
                    if(!(isNumber(e.keyCode) || isBackpaceEinfg(e.keyCode))) {
                        return;
                    }
                    autosuggest(suchbereich, suchItem, suggestItem);
                });
                suchItem.addEventListener("paste", function(e) {
                    // eine kurze Wartezeit, denn ansonsten würde nur der Wert _vor_ dem Pasten gefunden:
                    setTimeout(function(){
                        autosuggest(suchbereich, e.target, suggestItem);
                    }, 10);
                });            
                break;
            case 'ort':
                suchItem.addEventListener("keyup", function(e) {
                    if(!(isLetter(e.keyCode) || isBackpaceEinfg(e.keyCode))) {
                        return;
                    }
                    autosuggest(suchbereich, suchItem, suggestItem);
                });        
                break;            
            case 'str':
                suchItem.addEventListener("keyup", function(e) {
                    if(!(isLetter(e.keyCode) || isBackpaceEinfg(e.keyCode))) {
                        return;
                    }
                    autosuggest(suchbereich, suchItem, suggestItem);
                });        
                break;
            default:
                break;
        }
      
      suchItem.addEventListener("keydown", function(e) {
        var x = document.getElementById(this.id + "autocomplete-list");
        if (x) x = x.getElementsByTagName("div");
        if (e.keyCode == 40) {
          // arrowDown
          currentFocus++;
          addActive(x);
        } else if (e.keyCode == 38) {
          // arrowUp
          currentFocus--;
          addActive(x);
        } else if (e.keyCode == 13) {
          // ENTER
          e.preventDefault();
          if (currentFocus > -1) {
            // Klick simulieren: "Cannot read properties of undefined (reading 'click')" bei Eingabe von Enter ohne Liste
            if (x) x[currentFocus].click();
          }
        } else if (e.keyCode == 9) {
          // TAB
          if(suggestlisteOffen) {
            e.preventDefault();
            if (currentFocus > -1) {
                if (x) x[currentFocus].click();
            }        
          }
        }
        /*
        suchItem.addEventListener("blur", function (e) {
            closeAllLists();
        });
        */ 
    
        document.addEventListener("click", function (e) {
            closeAllLists();
        });    
    
      }); // /keydown listener
    } // / autocomplete function
    
    
    function autosuggest(suchbereich, suchItem, suggestItem) {
        var url;
    
        switch(suchbereich) {
            case 'plz': var zipCode = suchItem.value;
                        if(zipCode.length < 3 || zipCode.length > 5) {
                            closeAllLists();
                            return;
                        };
                        url = 'https://www.netcologne.de/api/dss-core-addresscheck/address-check/zipcodes?mode=LOCAL&zipCode=' + zipCode;
                        break;
            case 'ort': ///@weiter: funktioniert nicht, da es keinen cities-Endpunkt gibt
                        return;
    
                        
                        var ort = suchItem.value;
                        var zipCode = suggestItem.value;
                        if(ort.length < 3 || zipCode.length < 3 || zipCode.length > 5) return;
                        url = 'https://www.netcologne.de/api/dss-core-addresscheck/address-check/zipcodes?mode=LOCAL&zipCode=' + zipCode;
                        break;
            case 'str': var street = suchItem.value;
                        var zipCode = suggestItem.value;
                        if(street.length == 0) {
                            closeAllLists();
                            return;
                        };
                        if(zipCode.length !== 5) return;
                        url = 'https://www.netcologne.de/api/dss-core-addresscheck/address-check/streets?mode=LOCAL&zip=' + zipCode + '&street=' + street;
                        break;
            default: break;
        }
        var request = new XMLHttpRequest();
        request.open('GET', url, true);
        request.onload = function () {
            try{
                var results = JSON.parse(this.response);
                if (request.status >= 200 && request.status < 400) {
                    switch(suchbereich) {
                        case 'plz': adresscheck.plz = results; break;
                        case 'ort': adresscheck.ort = results; break;
                        case 'str': adresscheck.str = results; break;
                        default: break;
                    }
                } else {
                    console.warn('Unerwarteter AJAX-Status:', request.status, 'url=', url)
                }
            } catch{
                if (request.status !== 204)
                console.warn('Fehler bei onload: results=', results, 'status=', request.status)
            }
            suggestliste(suchbereich, suchItem, suggestItem);
        };
        request.send();
    }
    
    
    
    function suggestliste(suchbereich, input1, input2) {
    
        var divA, divB, i;
        var val = input1.value;
        closeAllLists();
    
        switch(suchbereich) {
            case 'plz' : autocompleteListe = adresscheck.plz; break;
            case 'ort' : autocompleteListe = adresscheck.plz; break;
            case 'str' : autocompleteListe = adresscheck.str; break;
        }
          if(!autocompleteListe) return false;
          
          suggestlisteOffen = true;
          
          divA = document.createElement("DIV");
          divA.setAttribute("id", input1.id + "autocomplete-list");
          divA.setAttribute("class", "autocomplete-items");
          input1.parentNode.appendChild(divA);
          var listValue;
          for (i = 0; i < autocompleteListe.length; i++) {
            var listValue;
            var helpValue;
            switch(suchbereich) {
                case 'plz' : listValue = autocompleteListe[i].zipCode; 
                             helpValue = autocompleteListe[i].city;
                             break;
                case 'ort' : listValue = autocompleteListe[i].city;
                             helpValue = autocompleteListe[i].zipCode;
                             break;
                case 'str' : listValue = autocompleteListe[i].street; 
                             break;
            }
    
            var foundAt = listValue.toUpperCase().indexOf(val.toUpperCase());
            var foundLength = val.length;
            if (foundAt >= 0) {
              divB = document.createElement("DIV");
              divB.innerHTML = listValue.substr(0, foundAt) +
                            "<strong>" + listValue.substr(foundAt, foundLength) + "</strong>" +
                            listValue.substr(foundAt + foundLength) +
                            (!!helpValue ? ' (' + helpValue + ')': '')
                            ;
              
              divB.innerHTML += '<input type="hidden" value="' + listValue + '"' +
                                (!!helpValue ? 'data-helpvalue="' + helpValue + '"' : '') +
                                '>';
              divB.addEventListener("click", function(e) {
                  // Wert aus der Liste zuweisen:
                  var clickedItem = this.getElementsByTagName("input")[0];
                  input1.value = clickedItem.value;
                  var helpValue = clickedItem.getAttribute('data-helpvalue');
                  if(!!helpValue) input2.value = helpValue;
                  closeAllLists();
              });
              divA.appendChild(divB);
    
              // Versuch, den Marker wieder herzustellen
              if(currentFocus == i) {
                  divB.classList.add("autocomplete-active"); /// ggf ersetzen durch Aufruf von addActive???
              }
    
            }
          }    
    }
    
    
    
    function removeActive(x) {
        for (var i = 0; i < x.length; i++) {
            x[i].classList.remove("autocomplete-active");
        }
    }
    
    function addActive(x) {
        if (!x) return false;
        if(!currentFocus) currentFocus = 0;
        removeActive(x);
        if (currentFocus >= x.length) {
           // wiederholt auf keydown gedrückt: erneut von oben starten
           currentFocus = 0; 
        };
        if (currentFocus < 0) currentFocus = (x.length - 1);
        try{
            x[currentFocus].classList.add("autocomplete-active"); //// @weiter: immer noch Cannot read properties of undefined (reading 'classList')
        } catch(e) {
            console.warn('Fehler bei classList. x=', x, 'currentFocus=', currentFocus)
        }
    }
    
    function closeAllLists() {
    //apex.jQuery('.autocomplete-items').remove();
        var x = document.getElementsByClassName("autocomplete-items");    
        for (var i = 0; i < x.length; i++) {
            //if (elmnt != x[i] && elmnt != input1) {
            if(true) {
                x[i].parentNode.removeChild(x[i]);
            }
        }
        currentFocus = 0;
        suggestlisteOffen = false;
    }
    
  execute-when-page-loads: |
    // Items für Autocomplete vorbereiten:
    autocomplete('plz', document.getElementById("P99100_PLZ"), document.getElementById("P99100_ORT"));
    autocomplete('ort', document.getElementById("P99100_ORT"), document.getElementById("P99100_PLZ"));
    autocomplete('str', document.getElementById("P99100_STRASSE"), document.getElementById("P99100_PLZ"));

css: 
  inline: |
    .autocomplete {
      /*the container must be positioned relative:*/
      position: relative;
      display: inline-block;
    }
    input {
      border: 1px solid transparent;
      background-color: #f1f1f1;
      padding: 10px;
      font-size: 16px;
    }
    input[type=text] {
      background-color: #f1f1f1;
      width: 100%;
    }
    input[type=submit] {
      background-color: DodgerBlue;
      color: #fff;
    }
    .autocomplete-items {
      position: absolute;
      border: 1px solid #d4d4d4;
      border-bottom: none;
      border-top: none;
      z-index: 99;
      /*position the autocomplete items to be the same width as the container:*/
      top: 100%;
      left: 0;
      right: 0;
    }
    .autocomplete-items div {
      padding: 10px;
      cursor: pointer;
      background-color: #fff;
      border-bottom: 1px solid #d4d4d4;
    }
    .autocomplete-items div:hover {
      /*when hovering an item:*/
      background-color: #e9e9e9;
    }
    .autocomplete-active {
      /*when navigating through the items using the arrow keys:*/
      background-color: DodgerBlue !important;
      color: #ffffff;
    }

security: 
  authentication: Page Requires Authentication
  deep-linking: Application Default
  page-access-protection: Arguments Must Have Checksum
  form-auto-complete: Off
  browser-cache: Application Default

session-management: 
  rejoin-sessions: Application Default

advanced: 
  enable-duplicate-page-submissions: Yes - Enable page to be re-posted
  reload-on-submit: Only for Success

server-cache: 
  caching: Disabled

configuration: 
  build-option: DEV oder TEST # 228976553402207959

comments: 
  comments: 'https://www.w3schools.com/howto/howto_js_autocomplete.asp'

regions: 
- # ====== Region: Test Autocomplete ===========================
  id: 235649415807458192
  identification: 
    name: Test Autocomplete
    type: Static Content

  layout: 
    sequence: 10
    parent-region: No Parent
    slot: BODY
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Blank with Attributes
    template-options: 
    - '#DEFAULT#'
    css-classes: 
    - autocomplete
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      output-as: HTML
      expand-shortcuts: false

- # ====== Region: L ===========================================
  id: 235652410737458222
  identification: 
    name: L
    type: Static Content

  layout: 
    sequence: 10
    parent-region: Test Autocomplete # 235649415807458192
    slot: SUB_REGIONS
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: 6

  appearance: 
    template: Blank with Attributes
    template-options: 
    - '#DEFAULT#'
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      output-as: HTML
      expand-shortcuts: false

- # ====== Region: M ===========================================
  id: 235652501168458223
  identification: 
    name: M
    type: Static Content

  layout: 
    sequence: 20
    parent-region: Test Autocomplete # 235649415807458192
    slot: SUB_REGIONS
    start-new-layout: false
    start-new-row: false
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Blank with Attributes
    template-options: 
    - '#DEFAULT#'
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      output-as: HTML
      expand-shortcuts: false

- # ====== Region: R ===========================================
  id: 235652618861458224
  identification: 
    name: R
    type: Static Content

  layout: 
    sequence: 30
    parent-region: Test Autocomplete # 235649415807458192
    slot: SUB_REGIONS
    start-new-layout: false
    start-new-row: false
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Blank with Attributes
    template-options: 
    - '#DEFAULT#'
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    settings: 
      output-as: HTML
      expand-shortcuts: false

- # ====== Region: Breadcrumb ==================================
  id: 235838620111729323
  identification: 
    name: Breadcrumb
    type: Breadcrumb

  source: 
    breadcrumb: Glascontainer # 234884631880092836

  layout: 
    sequence: 10
    parent-region: No Parent
    slot: REGION_POSITION_01
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic

  appearance: 
    template: Title Bar
    template-options: 
    - '#DEFAULT#'
    - t-BreadcrumbRegion--useBreadcrumbTitle
    render-components: Above Content

  accessibility: 
    use-landmark: true
    landmark-type: Template Default

  advanced: 
    region-display-selector: false
    exclude-title-from-translation: false

  customization: 
    customizable: Not Customizable By End Users

  attributes: 
    appearance: 
      breadcrumb-template: Breadcrumb
      template-options: 
      - '#DEFAULT#'

page-items: 
- # ====== Page Item: P99100_PLZ ===============================
  id: 235649494415458193
  identification: 
    name: P99100_PLZ
    type: Text Field

  label: 
    label: PLZ
    alignment: Left

  settings: 
    subtype: Text
    trim-spaces: Leading and Trailing
    text-case: NO CHANGE
    submit-when-enter-pressed: false
    disabled: false

  layout: 
    sequence: 10
    region: L # 235652410737458222
    slot: BODY
    alignment: Left
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic
    label-column-span: 4

  appearance: 
    template: Optional
    template-options: 
    - '#DEFAULT#'
    - t-Form-fieldContainer--large
    width: 30

  validation: 
    value-required: false

  advanced: 
    post-text-: '<span style="padding-left:2em">(mindestens 3 Ziffern für Adresscheck)</span>'
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    storage: Per Session (Persistent)

  quick-picks: 
    show-quick-picks: false

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

- # ====== Page Item: P99100_ORT ===============================
  id: 235649731096458195
  identification: 
    name: P99100_ORT
    type: Text Field

  label: 
    label: Ort
    alignment: Left

  settings: 
    subtype: Text
    trim-spaces: Leading and Trailing
    text-case: NO CHANGE
    submit-when-enter-pressed: false
    disabled: false

  layout: 
    sequence: 10
    region: M # 235652501168458223
    slot: BODY
    alignment: Left
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic
    label-column-span: 4

  appearance: 
    template: Optional
    template-options: 
    - '#DEFAULT#'
    - t-Form-fieldContainer--stretchInputs
    - t-Form-fieldContainer--large
    width: 30

  validation: 
    value-required: false

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    storage: Per Session (Persistent)

  quick-picks: 
    show-quick-picks: false

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

- # ====== Page Item: P99100_STRASSE ===========================
  id: 235649784580458196
  identification: 
    name: P99100_STRASSE
    type: Text Field

  label: 
    label: Straße
    alignment: Left

  settings: 
    subtype: Text
    trim-spaces: Leading and Trailing
    text-case: NO CHANGE
    submit-when-enter-pressed: false
    disabled: false

  layout: 
    sequence: 10
    region: R # 235652618861458224
    slot: BODY
    alignment: Left
    start-new-layout: false
    start-new-row: true
    column: Automatic
    new-column: true
    column-span: Automatic
    label-column-span: 4

  appearance: 
    template: Optional
    template-options: 
    - '#DEFAULT#'
    - t-Form-fieldContainer--stretchInputs
    - t-Form-fieldContainer--large
    width: 30

  validation: 
    value-required: false

  advanced: 
    warn-on-unsaved-changes: Page Default

  source: 
    type: Null
    used: Only when current value in session state is null

  session-state: 
    storage: Per Session (Persistent)

  quick-picks: 
    show-quick-picks: false

  security: 
    session-state-protection: Unrestricted
    store-value-encrypted-in-session-state: true
    restricted-characters: All characters can be saved.

